<!DOCTYPE html>
<html>
  <head>
    <title>Dissonance</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <script>
      window.clouds = "<%= image_url('clouds.svg') %>"
      window.logo = "<%= image_url('logo.png') %>"
      window.image_one = "<%= image_url('image_one.svg') %>"
      window.image_two = "<%= image_url('image_two.svg') %>"
      window.image_three = "<%= image_url('image_three.svg') %>"
      window.image_four = "<%= image_url('image_four.svg') %>"
      window.image_five = "<%= image_url('image_five.svg') %>"
      window.image_six = "<%= image_url('image_six.svg') %>"
      window.sparkles = "<%= image_url('sparkles.svg') %>"
    </script>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
